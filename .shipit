# auto-generated shipit config file.
# steps = FindVersion, ChangeVersion, CheckChangeLog, DistTest, Commit, Tag, MakeDist, UploadCPAN
steps = CheckChangeLog, DistTest, Commit, MakeDist, UploadCPAN

git.tagpattern = %v
git.push_to = origin
